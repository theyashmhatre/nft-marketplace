import React from "react";

const Card = ({title}) => {
    return(
        <div className="card">
        <div>
            <h1>{title}</h1>
        </div>

        </div>

    );
};
export default Card;